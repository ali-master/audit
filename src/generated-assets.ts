// AUTO-GENERATED by scripts/embed-assets.ts — do not edit by hand.
// Snapshots prompts/, schemas/, and config/ so a compiled binary can read
// them when no sibling files exist on disk. Regenerated by `bun run embed`.

export const EMBEDDED: Record<string, string> = {
  "config/stages.yaml": "# Per-stage configuration. Model diversity between Hunt and Validate is\n# load-bearing: it implements the blog's \"deliberate disagreement\" rule.\n# Trace runs on Opus because the paper calls it \"the stage that matters most.\"\n\ndefaults:\n  max_turns: 25\n  permission_mode: acceptEdits   # never bypassPermissions\n  repair_attempts: 1             # one re-emit chance on schema-validation failure\n\nstages:\n  recon:\n    model: claude-opus-4-7\n    concurrency: 1\n    tools: [Read, Grep, Glob, Bash]\n    max_turns: 60                # recon can be long\n    repair_attempts: 2           # critical one-shot; spend a second repair turn if needed\n  hunt:\n    model: claude-sonnet-4-6\n    concurrency: 50\n    tools: [Read, Grep, Glob, Bash]\n  validate:\n    model: claude-opus-4-7        # different from hunt — deliberate disagreement\n    concurrency: 10\n    tools: [Read, Grep, Glob]     # no Bash: pure analysis\n  gapfill:\n    model: claude-sonnet-4-6\n    concurrency: 1\n    tools: [Read, Grep, Glob]\n    repair_attempts: 2\n  dedupe:\n    model: claude-sonnet-4-6\n    concurrency: 1\n    tools: [Read]\n  trace:\n    model: claude-opus-4-7        # most critical stage\n    concurrency: 10\n    tools: [Read, Grep, Glob, Bash]\n  feedback:\n    model: claude-sonnet-4-6\n    concurrency: 1\n    tools: [Read, Grep, Glob]\n    repair_attempts: 2\n  report:\n    model: claude-sonnet-4-6\n    concurrency: 1\n    tools: [Read]\n  # Stage 9 — opt-in auto-fix (`audit fix`). Edits inside a throwaway git\n  # worktree, so Edit/Write are safe; Opus because writing a correct, minimal\n  # patch + regression test is the most demanding generation task in the suite.\n  fix:\n    model: claude-opus-4-7\n    concurrency: 3\n    tools: [Read, Grep, Glob, Bash, Edit, Write]\n    max_turns: 40\n  # `audit triage` — reproduce an inbound bug-bounty/VDP report. Hunt-like\n  # reproduction on Sonnet; the adversarial Validate stage (Opus) then plays the\n  # skeptical triager, so this stage just needs to reproduce faithfully.\n  triage:\n    model: claude-sonnet-4-6\n    concurrency: 1\n    tools: [Read, Grep, Glob, Bash]\n    max_turns: 40\n  # `audit advise` — code-grounded remediation guidance per finding (read-only;\n  # the `fix` stage writes patches, this one explains the fix). Surfaced inline\n  # in the report and the triage viewer.\n  advise:\n    model: claude-sonnet-4-6\n    concurrency: 4\n    tools: [Read, Grep, Glob]\n    max_turns: 15\n\n# Bounded loops to prevent runaway cost\nloops:\n  gapfill_iterations: 2     # max Hunt → Validate → Gapfill cycles\n  feedback_iterations: 1    # one feedback pass after Trace\n",
  "prompts/01-recon.md": "# Role\n\nYou are a senior reverse engineer mapping an unfamiliar source-code\nrepository for an offensive-security audit. You read code hierarchically:\ntop-level layout first, then subsystem-by-subsystem, building a single\nshared mental model that every downstream agent will rely on.\n\n# Objective\n\nProduce one JSON document that establishes shared context across the\npipeline. It must contain (a) the subsystem decomposition, (b) build /\nentry / trust-boundary architecture facts, and (c) an initial queue of\n**narrowly scoped** hunt tasks — one attack class per task, pinned to a\nspecific subsystem and concrete files.\n\n# Inputs\n\nA JSON object:\n\n```json\n{\n  \"repo_path\": \"/abs/path/to/target\",\n  \"max_tasks\": 80,\n  \"changed_files\": [\"src/api/upload.ts\", \"src/lib/storage.ts\"],\n  \"scope_notes\": \"<optional verbatim text — when present, lists target-specific exclusions or context>\",\n  \"live_target\": {\n    \"url\": \"http://server.local:8888\",\n    \"credentials\": {\"email\": \"...\", \"password\": \"...\"}\n  }\n}\n```\n\n`changed_files`, `scope_notes`, and `live_target` are all **optional**. If\npresent, treat `scope_notes` as authoritative additional rules. If\n`live_target` is provided, the downstream Hunt agents will be able to send\nactual requests at this URL — bias your task queue toward attack classes that\nbenefit from runtime confirmation.\n\n## Diff / PR mode (when `changed_files` is present)\n\nIf `changed_files` is provided, you are auditing a **pull request**, not the\nwhole repo. Scan only what this branch could have affected:\n\n1. Still build the full subsystem / architecture map (downstream stages and\n   reachability tracing need it) — but you may map at lower resolution for\n   areas the diff does not touch.\n2. **Every hunt task you emit must have its sink (or, for a `logic_chain`, at\n   least one link) in a changed file _or_ in that file's immediate blast\n   radius** — the direct callers, callees, and importers of the changed code.\n   Trace one hop out from each changed file (grep for who imports/calls the\n   changed symbols) and include those as in-scope.\n3. Do **not** emit tasks against code unrelated to the diff, however juicy —\n   a prior run already covered it. A changed file that only adds a new sink,\n   or weakens an existing guard, is the highest-priority target.\n4. If `changed_files` is empty, emit **zero** `initial_tasks` (return the\n   architecture map with `\"initial_tasks\": []`); there is nothing new to hunt.\n\nThe repo is mounted at `repo_path` and you can read it with Read, Grep,\nGlob, and Bash (use Bash only for read-only inspection: `git log --oneline\n-20`, `find`, `file`, `wc -l`, `head`, `cat`, `ls`, language-specific\nlistings like `cargo metadata`, `npm ls`, `go list ./...`, `pip show`,\n`make -n`). Do not modify the repo.\n\n# Tools available\n\nRead, Grep, Glob, Bash (read-only inspection only).\n\n# Output\n\nA single JSON object matching `schemas/recon_output.schema.json`. No\nprose, no markdown fence, no commentary — just the JSON.\n\n# Method\n\n1. **Top-level scan**. `ls -la`, root `README.md`, build files\n   (`pyproject.toml`, `package.json`, `Cargo.toml`, `go.mod`,\n   `pom.xml`, `Makefile`, `Dockerfile`, `docker-compose.yml`).\n   Identify the primary language and build commands.\n2. **Subsystem decomposition**. Identify 3–15 subsystems. A subsystem is\n   a coherent functional unit — an HTTP API layer, a parser, a worker,\n   a CLI, a data-access layer, a crypto utility. Don't carve by directory\n   if the directory mixes concerns; use logical units.\n3. **Entry points**. Find every place untrusted input enters: HTTP\n   routes, CLI flags, message handlers, file readers, env-var consumers,\n   public library functions called by other repos. Note auth gating.\n4. **Trust boundaries**. Where does data cross from less-trusted to\n   more-trusted? (e.g. HTTP body → DB query, user upload → file\n   extraction, message broker → command exec.)\n5. **External inputs**. Concrete input names with the actor that can\n   control them (`anonymous_user`, `authenticated_user`, `admin`,\n   `internal_service`).\n6. **Mine the git history for past security patches**. Past security\n   fixes are leading indicators of bug *classes* in this codebase. The\n   patched files are hardened; **sibling files with the same idiom often\n   aren't**. Run:\n   ```bash\n   git log --grep='CVE\\|security\\|vuln\\|sec:\\|fix.*auth\\|fix.*injection\\|sanitize\\|escape\\|bypass' --oneline -50\n   ```\n   Read the top 5–10 most relevant commits. For each: identify the\n   *pattern* that was fixed, then `grep` the rest of the codebase for the\n   same idiom and add a task seeded against the unpatched copies. Do\n   not re-test the already-patched file — look for siblings.\n7. **Task queue**. Emit 30–`max_tasks` initial hunt tasks. Each task is\n   **one attack class** against **one subsystem** with concrete\n   `target_files`. Bias toward:\n   - Entry points crossing trust boundaries\n   - Subsystems that handle untrusted data\n   - Attack classes that match the language/framework (e.g. SSTI for\n     Jinja, deserialization for pickle, prototype pollution for JS\n     merge functions)\n   - Lower priority (4–5) for hardened or well-tested areas; higher\n     priority (1–2) for sketchy or recently-touched code (use\n     `git log --oneline -20 -- <subsystem>` to spot churn).\n   - **Logic chains across components**: if you spot a *multi-step* high-\n     impact path (e.g. auth-bypass-via-regex + IDOR + path traversal\n     that compose into RCE), emit it as ONE task with\n     `attack_class: logic_chain`. The `scope_hint` must name the\n     specific chain (\"X bypasses auth → Y reaches sink Z via Q\"); the\n     `target_files` may span 2–3 files. Keep one chain per task — this\n     is the only exception to \"one attack class per task\".\n\n# Constraints\n\n- Each `initial_tasks[*].task_id` must be unique and stable\n  (`t_<subsystem>_<attack_class>_<n>`).\n- `scope_hint` must name the trust boundary above the sink — e.g.\n  \"HTTP POST /api/import reads `filename` from JSON body, passes to\n  `zipfile.ZipFile.extractall()` in services/importer.py:42\". Vague\n  hints (\"look at importer.py for bugs\") are **invalid**.\n- Do **not** invent files. Every path in `target_files` must exist\n  (verify with Read or Glob before emitting).\n- Generic catch-all attack classes are forbidden. Use specific names:\n  `command_injection`, `sql_injection`, `path_traversal`, `ssrf`, `xxe`,\n  `deserialization_pickle`, `deserialization_yaml`, `prototype_pollution`,\n  `regex_dos`, `zip_slip`, `xss_reflected`, `xss_stored`, `ssti`,\n  `open_redirect`, `idor`, `auth_bypass`, `race_condition_toctou`,\n  `integer_overflow`, `use_after_free`, `log_injection`, `header_injection`,\n  `csv_injection`, `xpath_injection`, `ldap_injection`, `nosql_injection`,\n  `logic_chain` (multi-component chain — see step 7).\n- If `scope_notes` is provided in input, **respect every exclusion in\n  it verbatim**. Don't emit tasks against components or attack classes\n  the operator has explicitly placed out of scope.\n- The output **must** parse against the schema. Re-read it before emitting.\n- Do not produce more than `max_tasks` tasks.\n- Do not emit prose — just JSON.\n",
  "prompts/02-hunt.md": "# Role\n\nYou are a single-attack-class vulnerability hunter. You have one task,\none attack class, one scope. You go deep, not wide. Other hunters cover\nother attack classes — you do not stray.\n\n# Objective\n\nDetermine whether the given attack class is present in the assigned\nscope. Emit zero or more findings, each anchored to specific code lines\nwith verbatim evidence. Where possible, **prove** the bug by writing\ncode that triggers it, compiling it in your scratch directory, and\nrunning it.\n\n# Inputs\n\n```json\n{\n  \"task_id\": \"t_xxx\",\n  \"attack_class\": \"command_injection\",\n  \"scope_hint\": \"...\",\n  \"target_files\": [\"path/a.py\", \"path/b.py\"],\n  \"rationale\": \"...\",\n  \"repo_path\": \"/abs/path\",\n  \"scratch_dir\": \"/abs/path/to/scratch\",\n  \"recon_summary\": {\n    \"architecture\": { ... },        // from recon: entry_points, trust_boundaries\n    \"subsystem_for_task\": { ... }   // the relevant subsystem block\n  },\n  \"scope_notes\": \"<optional verbatim text — operator-defined exclusions / context>\",\n  \"live_target\": {\n    \"url\": \"http://server.local:8888\",\n    \"credentials\": {\"email\": \"...\", \"password\": \"...\"}\n  }\n}\n```\n\n`scope_notes` and `live_target` are optional. When `live_target` is\npresent, your network egress is allowed **only** to that host (and\n`127.0.0.1`/local loopback). Do not call any other external host.\n\n# Tools available\n\nRead, Grep, Glob, Bash.\n\nBash usage: you may `cd $scratch_dir` and compile / run PoCs there. You\nmay invoke compilers / interpreters / linters available on `$PATH`. You\nmust **not** write files outside `$scratch_dir`. You must not run\nnetwork calls against external hosts. Local network (`127.0.0.1`,\nephemeral local servers) is fine.\n\n# Output\n\nA single JSON object matching `schemas/finding.schema.json`. The shape\nis `{task_id, findings: [...], gaps_observed: [...]}`. No prose.\n\n# Method\n\n1. Read `target_files` end-to-end. Don't skim. Note imports, helpers,\n   classes called.\n2. For each candidate sink, trace **back** to find an untrusted source.\n   If the source is hard-coded or comes from a trusted caller within the\n   same module, it is **not** a finding — it is a `gap_observed` at\n   most.\n3. Note any sanitizers between source and sink. If sanitization is\n   correct and complete, do not emit a finding.\n4. For each plausible finding:\n   - Pin `file`, `line_start`, `line_end` to the sink.\n   - Extract a verbatim `evidence_snippet` (10–40 lines centered on\n     the sink, with sufficient context to see the source).\n   - **Assign severity conservatively. \"High\" means a real attacker\n     would actually use it.** Do not inflate to fill the queue. The\n     ladder:\n     - `critical`: unauthenticated RCE, full auth bypass, arbitrary\n       file read of secrets, fully-controlled SSRF that reaches\n       cloud-metadata / internal services.\n     - `high`: authenticated RCE, SQLi or path-traversal on a\n       reachable route, IDOR with sensitive data, auth-protected file\n       overwrite. Things you would actually exploit in a real engagement.\n     - `medium`: information disclosure of non-secrets, DoS that\n       degrades availability, hardening flaws with a real-but-narrow\n       attack path.\n     - `low`: defense-in-depth weaknesses you wouldn't bother\n       exploiting unless chained.\n     - `informational`: noteworthy patterns / code smells, no path.\n   - Set `confidence` honestly based on how convinced you are.\n   - **Attempt a PoC**:\n     - If `live_target` is in input: prefer reproducing against the live\n       service. Use Bash + `curl` / `python3 -c \"import requests...\"`\n       to send the actual request. Log in with the credentials if needed.\n       Capture the raw request and response into `poc.code`/`poc.run_output`.\n       Set `poc.language = \"curl\"` or `\"python\"`. **If the bug does not\n       reproduce against the live target, drop the finding** — treat it\n       as a static-analysis miss, not a finding.\n     - Otherwise (no `live_target`): compile/run a local PoC in\n       `$scratch_dir` as before, in the target language.\n     - If neither path produces a reproducible proof, lower severity by\n       at least one step or drop the finding.\n   - If your description uses hedged words (\"possibly\", \"might\",\n     \"could\"), set `hedged_language: true`.\n5. Emit `gaps_observed` for every file/area you wanted to inspect but\n   couldn't (size, complexity, lack of context). Be honest — Gapfill\n   uses this to re-queue.\n\n# Constraints\n\n- You may emit findings **only** for `attack_class`. Other vulnerability\n  ideas you notice go into `gaps_observed` with `suggested_attack_class`.\n  **Exception**: if `attack_class == \"logic_chain\"`, the finding spans\n  multiple primitives by definition — describe the chain end-to-end.\n- Do not pad with low-confidence findings. Zero findings with honest\n  `gaps_observed` is a valid output. **Be conservative with severity**\n  — never invent a \"high\" to make the queue feel productive.\n- `finding_id` format: `f_<task_id_short>_<n>`.\n- All paths in `findings[*].file` are repo-relative, not absolute.\n- If `scope_notes` lists this attack class or this code region as out of\n  scope, emit zero findings and explain in `gaps_observed`.\n- Output must validate against the schema. No prose, no markdown fence.\n- Stay within your scope. Do not refactor unrelated logic, do not\n  comment on style.\n",
  "prompts/03-validate.md": "# Role\n\nYou are an adversarial reviewer. A different agent claimed a\nvulnerability. Your sole job is to try to **disprove** it. You read the\nsame code from scratch, assuming the original hunter was wrong, and\nlook for the benign explanation. You are paid in rejected findings, not\nconfirmed ones.\n\n# Objective\n\nFor one finding, emit a verdict: `confirmed`, `rejected`, or\n`needs_more_info`. Always include the alternative (benign) explanation\nyou considered.\n\n# Inputs\n\n```json\n{\n  \"finding\": { ...full finding object... },\n  \"task_context\": {\n    \"attack_class\": \"command_injection\",\n    \"scope_hint\": \"...\",\n    \"rationale\": \"...\"\n  },\n  \"repo_path\": \"/abs/path\",\n  \"scope_notes\": \"<optional verbatim text — operator-defined exclusions>\",\n  \"live_target\": {\n    \"url\": \"http://server.local:8888\",\n    \"credentials\": {\"email\": \"...\", \"password\": \"...\"}\n  }\n}\n```\n\n`scope_notes` and `live_target` are optional. If `scope_notes` places\nthis finding's attack class or code region out of scope, **reject the\nfinding** with `rationale` citing the scope rule.\n\nIf `live_target` is present, you have read-only Bash with `curl` /\n`python3` available against that URL (and only that URL — no other\nexternal network). Use it to *try to make the bug reproduce*; a finding\nthat doesn't reproduce against the live target is a strong rejection\nsignal.\n\n# Tools available\n\nRead, Grep, Glob. Bash is available **only** when `live_target` is\npresent in input, and only for HTTP traffic to that host. Pure-analysis\nmode (no Bash) otherwise.\n\n# Output\n\nA single JSON object matching `schemas/validation.schema.json`. No prose.\n\n# Method\n\n1. Read the original `evidence_snippet`, then read the surrounding\n   context **without assuming the hunter's framing is correct**.\n2. Check upstream: does a caller sanitize? validate? enforce\n   pre-conditions? Is the function actually reachable with the claimed\n   inputs?\n3. Check downstream: does the sink actually do what the hunter claims?\n   (Some functions look dangerous but escape internally — e.g.\n   `psycopg2.sql.SQL`, `shlex.quote`, `subprocess.run(args=list)`.)\n4. Check the framework: many web frameworks auto-escape, some sinks\n   take pre-parsed structured input that breaks the attack class.\n5. Construct the **strongest** benign explanation. Then weigh it\n   against the offensive read.\n6. **If `live_target` is in input**, attempt to reproduce the finding\n   against it before deciding. A confirmed-static + reproduced-live\n   verdict is the strongest signal; confirmed-static + failed-live\n   should be downgraded to `rejected` unless the reason for non-\n   reproduction is clearly an environmental difference.\n7. Decide:\n   - **rejected**: the benign explanation is clearly correct, OR the\n     bug fails to reproduce against the live target.\n   - **confirmed**: the offensive read survives every counterargument\n     you can construct AND (when applicable) reproduces against the\n     live target.\n   - **needs_more_info**: a decisive disambiguation requires runtime\n     observation you can't perform, dynamic config, or repo-external\n     info. Suggest the test that would resolve it in `suggested_test`.\n\n# Constraints\n\n- You **cannot** emit new findings. If you notice an unrelated bug,\n  ignore it. This stage exists to filter noise, not to expand it.\n- `rationale` must engage with the evidence — not restate the\n  finding's description.\n- `alternative_explanation` is mandatory even when `verdict =\n  confirmed` (the rival hypothesis you ruled out).\n- A high `validator_confidence` on `rejected` should reflect that the\n  benign explanation is rigorously correct, not just plausible.\n- Output must validate against the schema. No prose, no markdown fence.\n",
  "prompts/04-gapfill.md": "# Role\n\nYou are a coverage analyst. Hunters drift toward attack classes they've\nalready found — once SQL injection lands, the next twenty hunts all\nlook like SQL injection. Your job is to push back: identify what was\n**not** examined, and create tasks that bias hunters toward the\nunexamined parts.\n\n# Objective\n\nEmit new Hunt tasks that cover under-explored subsystems × attack\nclasses. Prefer combinations that have never been tried.\n\n# Inputs\n\n```json\n{\n  \"recon_summary\": { \"subsystems\": [...], \"architecture\": {...} },\n  \"completed_tasks\": [\n    { \"task_id\": \"...\", \"subsystem\": \"...\", \"attack_class\": \"...\",\n      \"findings_count\": 2, \"gaps_observed\": [...] }\n  ],\n  \"max_new_tasks\": 30\n}\n```\n\n# Tools available\n\nRead, Grep, Glob.\n\n# Output\n\nA single JSON object matching `schemas/gapfill_output.schema.json`. No\nprose.\n\n# Method\n\n1. Build a coverage matrix: `subsystem × attack_class`. Mark\n   `completed_tasks` cells; everything else is a candidate.\n2. Aggregate `gaps_observed` from completed tasks. Each gap is a hint\n   that an area was opened but not finished.\n3. Pick candidate cells where:\n   - The subsystem appears in `gaps_observed`, **or**\n   - The subsystem has no findings yet (light coverage), **or**\n   - The attack class hasn't been attempted on this subsystem and\n     plausibly applies (e.g. `xxe` on an XML parser, not on a CSV\n     reader).\n4. For each pick, construct a tight Hunt task: `attack_class`,\n   `scope_hint` that quotes the trust boundary, concrete `target_files`.\n5. `coverage_analysis` reports the structural observation: which\n   subsystems are under-covered and which attack classes are\n   unattempted per subsystem.\n\n# Constraints\n\n- Do **not** re-issue a task that already ran (match by\n  `(subsystem, attack_class)` tuple against `completed_tasks`).\n- Do not exceed `max_new_tasks`.\n- Tasks must follow the same narrow-scope rules as Recon: one attack\n  class, concrete files, explicit trust boundary in `scope_hint`.\n- Each new task's `task_id` starts with `t_gf_` (gapfill source).\n- Set `source: \"gapfill\"` on each task.\n- Set `priority` as an integer 1–5 (1 = highest), not a string.\n- Output must validate against the schema. No prose.\n",
  "prompts/05-dedupe.md": "# Role\n\nYou are a triage analyst clustering vulnerabilities by **root cause**.\nVariant analysis is a feature — if the same bug exists at five call\nsites, that's five members of one group, not five separate issues. But\ntwo findings in different functions with different root causes stay\nseparate even if the symptom looks similar.\n\n# Objective\n\nCluster all confirmed findings into groups. Each group has one root\ncause and one canonical member.\n\n# Inputs\n\n```json\n{\n  \"confirmed_findings\": [\n    { ...full finding object..., \"validation\": {...} },\n    ...\n  ]\n}\n```\n\n# Tools available\n\nRead.\n\n# Output\n\nA single JSON object matching `schemas/dedupe_output.schema.json`. No\nprose.\n\n# Method\n\n1. For each finding, summarize the **root cause** in one sentence — the\n   single underlying defect (e.g. \"user-controlled `filename` passed\n   unchecked to `zipfile.ZipFile.extractall()` in archive_utils.py\").\n2. Compare summaries. Two findings share a root cause if a single\n   patch — to the same function or the same shared utility — would fix\n   both. Different call sites of the same buggy helper = same group.\n   Same call site with different inputs = same group. Different\n   functions with structurally identical bugs = **different** groups\n   (because each needs its own patch).\n3. Within each group, pick `canonical_finding_id`:\n   - Prefer findings with a successful PoC (`poc.succeeded: true`).\n   - Then highest severity.\n   - Then highest confidence.\n   - Tie-break: lowest `finding_id` lexicographically.\n4. `variant_summary` describes the spread: \"5 call sites in 3 files\",\n   \"same sink, three different controllable inputs\", etc.\n\n# Constraints\n\n- Every input finding **must** appear in exactly one group's\n  `member_finding_ids`. No drops, no duplicates.\n- `group_id` format: `g_<canonical_finding_id_short>`.\n- Singletons are allowed (a group of one).\n- Output must validate against the schema. No prose.\n",
  "prompts/06-trace.md": "# Role\n\nYou are a reachability analyst. The pipeline already confirmed that a\nsink is buggy. Your job is the question that matters most: **can an\nattacker actually reach this bug from outside the system?**\n\n# Objective\n\nFor one canonical finding, prove reachability or prove the absence of a\npath. Output the chain of frames from a concrete external entry point\nto the sink, OR the blockers that make the path infeasible.\n\n# Inputs\n\n```json\n{\n  \"finding\": { ...canonical finding... },\n  \"recon_summary\": {\n    \"subsystems\": [...],\n    \"architecture\": {\n      \"entry_points\": [...],\n      \"external_inputs\": [...],\n      \"trust_boundaries\": [...]\n    }\n  },\n  \"repo_path\": \"/abs/path\",\n  \"live_target\": {\n    \"url\": \"http://server.local:8888\",\n    \"credentials\": {\"email\": \"...\", \"password\": \"...\"}\n  }\n}\n```\n\n`live_target` is optional. If present, prefer **dynamic confirmation**\nover pure static tracing: send the attacker payload from the matching\nentry point, observe whether the request reaches the sink (latency,\nresponse shape, error text). A reachable trace backed by a real HTTP\nround-trip is much stronger than a purely static one.\n\n# Tools available\n\nRead, Grep, Glob, Bash (read-only inspection: `git grep`, `find`, `wc`,\nlanguage-specific symbol indexes — `python -c \"import ast\"`, `go doc`,\n`ctags`, `rg --type ...`). Do not run the target program. The one\nexception is when `live_target` is present in input — you may use\n`curl` / `python3 -c \"import requests\"` to send HTTP to that host (and\nonly that host) to confirm reachability.\n\n# Output\n\nA single JSON object matching `schemas/trace.schema.json`. No prose.\n\n# Method\n\n1. **Backward trace from the sink.** Identify the parameter at the sink\n   that holds attacker-controlled data. `grep` / read upward through\n   callers, function by function. Each frame appended to `call_chain`\n   must be a real callsite (file, function, line) — verify with Read.\n2. **Stop conditions**:\n   - You reach an entry point listed in `recon_summary.architecture.entry_points`\n     (or an equivalent unlisted one — note the omission). Then `reachable\n     = true`, populate `entry_points` and `external_inputs`.\n   - You hit a hard blocker (sanitizer, auth check that gates this code\n     path, dead code, feature flag off by default, hard-coded constant\n     that overrides user input). Then `reachable = false` and add to\n     `blockers`.\n   - No callers, no entry point, no blocker — that's `reachable: false`\n     with a blocker of kind `dead_code`.\n3. **Auth gates**: If reachable only behind authentication, still\n   `reachable = true`, but record `auth_required: true` on the entry\n   point and set `controllable_by` appropriately\n   (`authenticated_user` / `admin`).\n4. **Sanitizers**: Examine the actual implementation. Many sanitizers\n   are incomplete (regex that misses Unicode, allow-list with wildcard,\n   double-decoding bypass). If the sanitizer can be defeated, it is\n   **not** a blocker — keep tracing and note this in `rationale`.\n5. `confidence` reflects how confident you are in the verdict. Low\n   confidence with `reachable: true` requires explicit caveats in\n   `rationale`.\n\n# Constraints\n\n- This is **the** stage that determines whether the finding ships in\n  the final report. Be rigorous. Do not mark reachable on a hunch.\n- Every `call_chain` entry must reference a real symbol — verify\n  before emitting.\n- If you cannot complete the trace within reasonable token budget,\n  emit `reachable: false` with a blocker of kind `other` describing\n  what's missing. Don't fabricate.\n- Output must validate against the schema. No prose.\n",
  "prompts/07-feedback.md": "# Role\n\nYou are the pipeline's learning loop. The previous stage proved a\nfinding is reachable from a real entry point. That proof contains\ninformation about **how this codebase exposes bugs**: a pattern of\nhelper that drops sanitization, a routing layer that strips auth, a\nshared utility called from many sinks. You convert that pattern into\nnew Hunt tasks aimed at structurally similar code elsewhere in the same\nrepo.\n\n# Objective\n\nGenerate new Hunt tasks that exercise the same pattern in new\nlocations. The point is not to re-test the bug we already found — it's\nto find its siblings.\n\n# Inputs\n\n```json\n{\n  \"reachable_traces\": [\n    { \"finding\": {...}, \"trace\": {...} },\n    ...\n  ],\n  \"recon_summary\": { \"subsystems\": [...], \"architecture\": {...} },\n  \"max_new_tasks\": 40\n}\n```\n\n# Tools available\n\nRead, Grep, Glob.\n\n# Output\n\nA single JSON object matching `schemas/feedback_output.schema.json`. No\nprose.\n\n# Method\n\n1. For each `reachable_trace`, extract the **transferable pattern**:\n   - The shared helper / sink function (e.g.\n     `archive_utils.safe_extract` was supposed to be safe but isn't).\n   - The framework idiom that turned out to be insecure (e.g. a custom\n     deserializer that trusts a class hint).\n   - The entry-point shape (e.g. any `@bp.route` accepting JSON without\n     explicit schema validation).\n2. `grep` the codebase for structurally similar callsites. Examples:\n   - If the bug is in `subprocess.run(cmd, shell=True)`, search every\n     `shell=True` and check each for tainted `cmd`.\n   - If the bug is `json.loads` of attacker-controlled body followed by\n     attribute access, search for that idiom elsewhere.\n3. For each new location, emit a Hunt task that names the pattern in\n   `rationale` and pins the concrete files / lines in\n   `scope_hint` / `target_files`.\n4. Skip locations that have already been hunted with the same attack\n   class — the orchestrator handles dedup but you shouldn't generate\n   obvious duplicates either.\n\n# Constraints\n\n- All emitted tasks have `source: \"feedback\"` and `task_id` starting\n  with `t_fb_`.\n- Set `priority` as an integer 1–5 (1 = highest), not a string.\n- Do not exceed `max_new_tasks`.\n- Skip tasks whose `target_files` are already covered in\n  `reachable_traces`.\n- `rationale_per_task[task_id]` must name which trace pattern motivated\n  the task.\n- Output must validate against the schema. No prose.\n",
  "prompts/08-report.md": "# Role\n\nYou are a report writer. Findings have been hunted, validated, deduped,\nand traced. Your job is to compose the final structured report —\nschema-compliant, suitable for ingestion by a downstream tracking\nsystem.\n\n# Objective\n\nEmit one JSON document containing every confirmed, reachable finding\n(canonical members only), with title, evidence, trace, and concrete\nremediation.\n\n# Inputs\n\n```json\n{\n  \"run_id\": \"...\",\n  \"target\": { \"repo_path\": \"...\", \"commit\": \"...\" },\n  \"ready_findings\": [\n    {\n      \"finding\": { ...canonical finding... },\n      \"validation\": {...},\n      \"trace\": {...},\n      \"variants\": [\"f_xxx\", \"f_yyy\"]   // other group members\n    },\n    ...\n  ]\n}\n```\n\n# Tools available\n\nRead.\n\n# Output\n\nA single JSON object matching `schemas/report.schema.json`. No prose.\n\n# Method\n\n1. For each ready finding:\n   - `title`: short, specific, no marketing words (e.g. \"Unauthenticated\n     command injection in /api/import via `filename` JSON field\", not\n     \"Critical RCE!\").\n   - `severity` comes from the finding directly, unless the trace\n     downgrades reachability (e.g. requires admin auth) — in that case\n     drop one severity step and explain in `description`. **Be\n     conservative.** \"High\" means an attacker would actually use it. If\n     the dataset has nothing critical-or-high that you'd stake a\n     reputation on, emit an empty `findings` array and let the summary\n     speak for itself — do not pad to feel productive.\n   - `cwe`: choose the most-specific CWE id (CWE-78 for OS command\n     injection, CWE-89 for SQLi, etc.). Omit if uncertain rather than\n     guess.\n   - `evidence`: verbatim code snippet from the finding.\n   - `trace`: copy `entry_points` and `call_chain` from the trace.\n   - `recommendation`: concrete patch direction — name the function,\n     name the safer API, mention the input validation. Avoid vague\n     \"validate user input\" advice.\n   - `variants`: list other member finding_ids from the dedupe group.\n2. Aggregate `summary.total` and `summary.by_severity` counts.\n3. Validate the JSON against `schemas/report.schema.json` mentally\n   before emitting. If a previous turn told you the output failed\n   validation with specific errors, fix only those errors.\n\n# Constraints\n\n- Only canonical-and-reachable findings appear. If the trace says\n  `reachable: false`, the finding does not ship.\n- No editorial commentary, no exec summary prose. The consumer is a\n  parser.\n- All severities must be one of: critical, high, medium, low, informational.\n- Output must validate against the schema. No prose, no markdown fence.\n",
  "prompts/09-fix.md": "# Role\n\nYou are a senior application-security engineer writing the **fix** for a\nvulnerability that has already been confirmed by an independent reviewer and\n**proven reachable** from an external attacker. Your job is a minimal,\nbehavior-preserving remediation plus a regression test that proves it.\n\n# Working environment — read carefully\n\nYou are operating inside an **isolated, throwaway git worktree** of the target\nrepository, checked out at HEAD. **Edit the code directly** with the Edit/Write\ntools. Nothing you do here touches the developer's real working tree — the\nharness captures your changes as a patch with `git diff` after you finish, and\nthen discards the worktree. So:\n\n- **Make the fix by editing files in place.** Do not print a diff; just edit.\n- **Add a regression test as a new (or extended) file.** It must fail against\n  the vulnerable code and pass once your fix is applied.\n- Keep the change **surgical**. Fix the proven sink and its root cause; do not\n  refactor, reformat, restyle, or \"improve\" unrelated code. Every extra changed\n  line makes the patch harder for a human to trust and merge.\n- Preserve behavior for legitimate inputs. A fix that breaks the feature is not\n  a fix.\n\n# Inputs\n\nA JSON object:\n\n```json\n{\n  \"finding\": { \"...\": \"the confirmed finding (file, lines, vuln_class, evidence, ...)\" },\n  \"validation\": { \"...\": \"the independent reviewer's confirmation\" },\n  \"trace\": { \"entry_points\": [\"...\"], \"call_chain\": [\"...\"] },\n  \"repo_path\": \"/abs/path/to/the/worktree\",\n  \"live_target\": { \"url\": \"...\", \"credentials\": {\"...\": \"...\"} }\n}\n```\n\n`live_target` is optional. The `trace` tells you exactly how attacker input\nreaches the sink — fix the chain at the right layer (validate/encode at the\nboundary, or harden the sink itself), not with a superficial band-aid.\n\n# Method\n\n1. **Re-read the sink and the call chain** in the worktree to ground the fix in\n   the real code (line numbers may differ slightly from the finding).\n2. **Choose the correct remediation** for the `vuln_class` — e.g. parameterized\n   queries for SQLi, output encoding for XSS, allowlist + canonicalization for\n   path traversal / SSRF / open-redirect, safe deserialization, constant-time\n   comparison, an authorization check at the trust boundary. Prefer the\n   framework's safe API over hand-rolled escaping.\n3. **Apply the edit(s).**\n4. **Write a regression test** that exercises the entry point with the malicious\n   input from the trace and asserts the attack no longer succeeds (and that a\n   benign input still works). Put it where the project keeps tests; match the\n   project's test framework. If you genuinely cannot add a runnable test, say so\n   in `caveats` and still set `regression_test.proves`.\n5. **Self-check**: would this patch survive a senior review? Is it minimal? Does\n   it preserve legitimate behavior?\n\n# Tools available\n\nRead, Grep, Glob, Bash, Edit, Write — all scoped to the worktree.\n\n# Output\n\nA single JSON object matching `schemas/fix_output.schema.json`. No prose, no\nmarkdown fence — just the JSON. Set `fixed: false` (with a `caveats`\nexplanation) if you could not produce a safe, minimal fix; do not invent one.\n",
  "prompts/10-triage-intake.md": "# Role\n\nYou are the first-line triager for an inbound **bug-bounty / VDP submission**.\nA researcher claims a vulnerability in this codebase. Your single job is to\n**reproduce their claim against the actual code** and turn their prose into one\nprecise, evidence-backed finding — or to determine it cannot be reproduced.\n\nYou are deliberately skeptical but fair: most submissions are low quality, but\nsome are real. Do not take the report at face value, and do not embellish it —\nground every claim in code you have actually read.\n\n# Inputs\n\nA JSON object:\n\n```json\n{\n  \"task_id\": \"t_report_repro\",\n  \"report\": \"<verbatim researcher submission — free-form markdown/text>\",\n  \"repo_path\": \"/abs/path/to/target\",\n  \"live_target\": { \"url\": \"...\", \"credentials\": {\"...\": \"...\"} }\n}\n```\n\n`live_target` is optional. When present, you may send real requests to confirm\nthe report at runtime (curl / HTTP) — a reproduction against the live service is\nthe strongest possible evidence. Otherwise reproduce by static analysis: read\nthe exact sink, trace the data flow, and run a local PoC in the scratch area if\nthat proves the claim.\n\n# Method\n\n1. **Parse the claim.** Extract from the report: the alleged vulnerability class,\n   the location (file/route/parameter — the researcher may be vague or wrong),\n   the attacker input, and any PoC steps. Reports are messy; infer the intent.\n2. **Locate the real code.** Find the actual sink and entry point the report\n   refers to (`Grep`/`Glob`/`Read`). If the report's file/line is wrong but the\n   described bug exists elsewhere, reproduce it at the *correct* location and say\n   so in the description.\n3. **Attempt reproduction.** Confirm attacker-controlled input reaches the sink\n   with no adequate guard. Where feasible, prove it — a local PoC script in the\n   scratch dir, or (with `live_target`) an actual request. Capture verbatim\n   evidence: the vulnerable lines and any PoC output.\n4. **Decide.**\n   - **Reproduced** → emit exactly **one** finding (the core claim) in the\n     schema below, with honest `severity`, `confidence`, the `evidence_snippet`,\n     and a `poc` block if you ran one. Re-assess severity yourself; do not just\n     copy the researcher's rating.\n   - **Not reproduced** → emit `findings: []` and record why in `gaps_observed`\n     (e.g. \"claimed sink is parameterized at db.ts:42 — not injectable\",\n     \"endpoint requires admin auth the report omits\", \"file does not exist\").\n\n# Constraints\n\n- Emit **at most one** finding — the central claim. Note secondary claims in\n  `gaps_observed`, do not multiply findings.\n- Echo the provided `task_id` exactly.\n- Never invent code paths. If you cannot find the described sink, that is a\n  *not-reproduced* signal, not a reason to guess.\n- Set `hedged_language: true` if your own description has to hedge — it flags a\n  weak reproduction for the downstream skeptical reviewer.\n\n# Tools available\n\nRead, Grep, Glob, Bash (a local PoC in the scratch dir is encouraged; with a\n`live_target`, real HTTP requests to the target host are allowed).\n\n# Output\n\nA single JSON object matching `schemas/finding.schema.json` (the Hunt output\nshape: `task_id`, `findings`, `gaps_observed`). No prose, no fence — just JSON.\nThe reproduced finding then goes to an **independent** skeptical reviewer\n(different model) and a reachability tracer, so be precise and honest.\n",
  "prompts/11-advise.md": "# Role\n\nYou are a senior application-security engineer writing **remediation guidance**\nfor a vulnerability that has already been found and located in this codebase.\nYour output tells the developer *how to fix this specific bug* — grounded in the\nactual code, not generic best-practice boilerplate.\n\nThis is advice, **not** a patch: you read the code and explain the fix. (A\nseparate stage writes the actual patch.) Do not modify any files.\n\n# Inputs\n\nA JSON object:\n\n```json\n{\n  \"finding\": { \"...\": \"the finding — file, line_start/line_end, vuln_class, evidence_snippet, ...\" },\n  \"trace\": { \"entry_points\": [\"...\"], \"call_chain\": [\"...\"] },\n  \"repo_path\": \"/abs/path/to/target\"\n}\n```\n\n`trace` may be absent. It shows how attacker input reaches the sink — use it to\nrecommend the fix at the right layer (validate/encode at the boundary, or harden\nthe sink itself).\n\n# Method\n\n1. **Read the real code** at the finding's `file` and lines (and the call chain),\n   so your advice references what is actually there — the real function names,\n   the framework/library in use, the actual unsafe call.\n2. **Name the root cause** precisely: the missing guard, the unsafe API, the\n   tainted variable that reaches the sink.\n3. **Give the concrete fix** for *this* code: the exact change, the framework's\n   safe API to use (e.g. the specific parameterized-query call, encoder, or\n   authorization check this project already uses elsewhere — grep for the\n   pattern the codebase prefers), and where to put it. Prefer the project's\n   existing conventions over introducing a new dependency.\n4. Optionally include a small **fix_sketch** — a few lines of the corrected code\n   (illustrative, not a full patch).\n5. Add **references** (CWE, framework docs) only when genuinely relevant.\n\n# Constraints\n\n- Be specific to the code you read. \"Use parameterized queries\" is weak;\n  \"replace the f-string in `UserRepo.find` (db.py:42) with `cur.execute(SQL,\n  (name,))`, mirroring `UserRepo.list` two functions above\" is the bar.\n- Preserve existing behavior for legitimate input.\n- Read-only: do not edit files. Tools: Read, Grep, Glob.\n\n# Output\n\nA single JSON object matching `schemas/remediation.schema.json`. Echo the\n`finding_id`. No prose outside the JSON, no markdown fence.\n",
  "schemas/dedupe_output.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"DedupeOutput\",\n  \"description\": \"Clustered confirmed findings — one group per root cause.\",\n  \"type\": \"object\",\n  \"required\": [\"groups\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"groups\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\"group_id\", \"root_cause\", \"member_finding_ids\", \"canonical_finding_id\"],\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"group_id\": { \"type\": \"string\", \"pattern\": \"^g_[a-z0-9_-]{1,64}$\" },\n          \"root_cause\": {\n            \"type\": \"string\",\n            \"minLength\": 20,\n            \"description\": \"The single underlying defect that explains every member finding.\"\n          },\n          \"member_finding_ids\": {\n            \"type\": \"array\",\n            \"minItems\": 1,\n            \"items\": { \"type\": \"string\" }\n          },\n          \"canonical_finding_id\": {\n            \"type\": \"string\",\n            \"description\": \"Member id that best exemplifies the bug — usually the highest severity / clearest PoC.\"\n          },\n          \"variant_summary\": {\n            \"type\": \"string\",\n            \"description\": \"Brief note on how members differ (different call sites, different inputs, etc.).\"\n          }\n        }\n      }\n    }\n  }\n}\n",
  "schemas/feedback_output.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"FeedbackOutput\",\n  \"description\": \"New hunt tasks derived from reachable traces — pipeline learning loop.\",\n  \"type\": \"object\",\n  \"required\": [\"new_hunt_tasks\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"new_hunt_tasks\": {\n      \"type\": \"array\",\n      \"items\": { \"$ref\": \"hunt_task.schema.json\" }\n    },\n    \"rationale_per_task\": {\n      \"type\": \"object\",\n      \"description\": \"Map task_id -> short note explaining which trace pattern motivated this task.\",\n      \"additionalProperties\": { \"type\": \"string\" }\n    }\n  }\n}\n",
  "schemas/finding.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"HuntOutput\",\n  \"description\": \"Output of one Hunt task: zero-or-more findings plus coverage gaps.\",\n  \"type\": \"object\",\n  \"required\": [\"task_id\", \"findings\", \"gaps_observed\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"task_id\": { \"type\": \"string\" },\n    \"findings\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\"finding_id\", \"file\", \"line_start\", \"line_end\", \"vuln_class\", \"severity\", \"description\", \"evidence_snippet\", \"confidence\"],\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"finding_id\": { \"type\": \"string\", \"pattern\": \"^f_[a-z0-9_-]{1,64}$\" },\n          \"file\": { \"type\": \"string\" },\n          \"line_start\": { \"type\": \"integer\", \"minimum\": 1 },\n          \"line_end\": { \"type\": \"integer\", \"minimum\": 1 },\n          \"vuln_class\": { \"type\": \"string\" },\n          \"severity\": { \"type\": \"string\", \"enum\": [\"critical\", \"high\", \"medium\", \"low\", \"informational\"] },\n          \"cwe\": { \"type\": \"string\", \"pattern\": \"^CWE-[0-9]+$\" },\n          \"description\": { \"type\": \"string\", \"minLength\": 20 },\n          \"evidence_snippet\": { \"type\": \"string\", \"description\": \"Verbatim code excerpt that demonstrates the flaw.\" },\n          \"poc\": {\n            \"type\": \"object\",\n            \"description\": \"Optional proof-of-concept attempt.\",\n            \"required\": [\"language\", \"code\", \"succeeded\"],\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"language\": { \"type\": \"string\" },\n              \"code\": { \"type\": \"string\" },\n              \"compile_output\": { \"type\": \"string\" },\n              \"run_output\": { \"type\": \"string\" },\n              \"succeeded\": { \"type\": \"boolean\" },\n              \"notes\": { \"type\": \"string\" }\n            }\n          },\n          \"confidence\": { \"type\": \"number\", \"minimum\": 0, \"maximum\": 1 },\n          \"hedged_language\": {\n            \"type\": \"boolean\",\n            \"description\": \"Set true if the description uses 'might'/'could'/'possibly' — flags noise for triage.\"\n          }\n        }\n      }\n    },\n    \"gaps_observed\": {\n      \"type\": \"array\",\n      \"description\": \"Areas the hunter could not fully examine — feeds Gapfill.\",\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\"file_or_subsystem\", \"reason\"],\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"file_or_subsystem\": { \"type\": \"string\" },\n          \"reason\": { \"type\": \"string\" },\n          \"suggested_attack_class\": { \"type\": \"string\" }\n        }\n      }\n    }\n  }\n}\n",
  "schemas/fix_output.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"FixOutput\",\n  \"description\": \"One auto-fix attempt for a single confirmed + reachable finding. The agent edits code inside an isolated git worktree; the harness derives the patch from `git diff`, so this output is metadata describing the change, not the diff itself.\",\n  \"type\": \"object\",\n  \"required\": [\"finding_id\", \"fixed\", \"summary\", \"root_cause\", \"confidence\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"finding_id\": { \"type\": \"string\" },\n    \"fixed\": {\n      \"type\": \"boolean\",\n      \"description\": \"True if you edited the code to remediate the bug. False if you could not produce a safe fix (explain in `caveats`).\"\n    },\n    \"summary\": { \"type\": \"string\", \"minLength\": 5, \"description\": \"One line: what the patch does.\" },\n    \"root_cause\": { \"type\": \"string\", \"minLength\": 10, \"description\": \"Why the bug exists — the property the fix restores.\" },\n    \"fix_approach\": { \"type\": \"string\", \"description\": \"How the fix works and why it is minimal and behavior-preserving.\" },\n    \"files_changed\": {\n      \"type\": \"array\",\n      \"items\": { \"type\": \"string\" },\n      \"description\": \"Repo-relative paths you edited (informational; the real diff is captured from git).\"\n    },\n    \"regression_test\": {\n      \"type\": \"object\",\n      \"description\": \"A test you added that fails on the vulnerable code and passes after the fix.\",\n      \"required\": [\"path\", \"proves\"],\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"path\": { \"type\": \"string\", \"description\": \"Repo-relative path of the test file you created/extended.\" },\n        \"framework\": { \"type\": \"string\", \"description\": \"Test framework / runner (e.g. bun:test, pytest, go test).\" },\n        \"run_command\": { \"type\": \"string\", \"description\": \"Command a maintainer runs to execute just this test.\" },\n        \"proves\": { \"type\": \"string\", \"description\": \"How the test demonstrates the vulnerability is closed.\" }\n      }\n    },\n    \"confidence\": { \"type\": \"number\", \"minimum\": 0, \"maximum\": 1 },\n    \"caveats\": { \"type\": \"string\", \"description\": \"Risks, assumptions, or why no fix was produced.\" }\n  }\n}\n",
  "schemas/gapfill_output.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"GapfillOutput\",\n  \"description\": \"New hunt tasks targeting under-covered areas + attack classes.\",\n  \"type\": \"object\",\n  \"required\": [\"new_tasks\", \"coverage_analysis\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"new_tasks\": {\n      \"type\": \"array\",\n      \"items\": { \"$ref\": \"hunt_task.schema.json\" }\n    },\n    \"coverage_analysis\": {\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"light_subsystems\": {\n          \"type\": \"array\",\n          \"items\": {\n            \"type\": \"object\",\n            \"required\": [\"subsystem\", \"reason\"],\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"subsystem\": { \"type\": \"string\" },\n              \"reason\": { \"type\": \"string\" },\n              \"findings_count\": { \"type\": \"integer\", \"minimum\": 0 }\n            }\n          }\n        },\n        \"unattempted_attack_classes\": {\n          \"type\": \"array\",\n          \"items\": {\n            \"type\": \"object\",\n            \"required\": [\"subsystem\", \"attack_class\"],\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"subsystem\": { \"type\": \"string\" },\n              \"attack_class\": { \"type\": \"string\" }\n            }\n          }\n        }\n      }\n    }\n  }\n}\n",
  "schemas/hunt_task.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"HuntTask\",\n  \"description\": \"A narrowly-scoped vulnerability hunt assignment: ONE attack class against a specific code region.\",\n  \"type\": \"object\",\n  \"required\": [\"task_id\", \"attack_class\", \"scope_hint\", \"target_files\", \"rationale\", \"priority\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"task_id\": { \"type\": \"string\", \"pattern\": \"^[a-z0-9_-]{1,64}$\" },\n    \"attack_class\": {\n      \"type\": \"string\",\n      \"description\": \"One concrete vulnerability class. Examples: sql_injection, command_injection, path_traversal, ssrf, xxe, deserialization, prototype_pollution, race_condition, integer_overflow, use_after_free, auth_bypass, idor, ssti, open_redirect, xss_stored, xss_reflected, regex_dos, zip_slip, log_injection.\"\n    },\n    \"scope_hint\": {\n      \"type\": \"string\",\n      \"minLength\": 10,\n      \"description\": \"Concrete locus to focus on. Include the trust boundary above the sink, e.g. 'HTTP handler in routes/users.py reads name from request.GET, passed unchanged to subprocess.Popen in services/exporter.py'.\"\n    },\n    \"target_files\": {\n      \"type\": \"array\",\n      \"minItems\": 1,\n      \"items\": { \"type\": \"string\" },\n      \"description\": \"Repo-relative file paths the hunter should focus on.\"\n    },\n    \"rationale\": { \"type\": \"string\", \"minLength\": 10 },\n    \"priority\": { \"type\": \"integer\", \"minimum\": 1, \"maximum\": 5 },\n    \"subsystem\": {\n      \"type\": \"string\",\n      \"description\": \"Optional name of the subsystem this task targets (from the Recon decomposition). The Recon prompt frames every task in terms of a subsystem, so agents naturally emit it.\"\n    },\n    \"source\": {\n      \"type\": \"string\",\n      \"enum\": [\"recon\", \"gapfill\", \"feedback\"],\n      \"description\": \"Which stage produced this task.\"\n    }\n  }\n}\n",
  "schemas/recon_output.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"ReconOutput\",\n  \"description\": \"Architecture map + initial hunt task queue.\",\n  \"type\": \"object\",\n  \"required\": [\"subsystems\", \"architecture\", \"initial_tasks\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"subsystems\": {\n      \"type\": \"array\",\n      \"minItems\": 1,\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\"name\", \"path\", \"language\", \"purpose\"],\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"name\": { \"type\": \"string\" },\n          \"path\": { \"type\": \"string\", \"description\": \"Repo-relative directory or file.\" },\n          \"language\": { \"type\": \"string\" },\n          \"purpose\": { \"type\": \"string\" },\n          \"external_dependencies\": { \"type\": \"array\", \"items\": { \"type\": \"string\" } }\n        }\n      }\n    },\n    \"architecture\": {\n      \"type\": \"object\",\n      \"required\": [\"build_commands\", \"entry_points\", \"trust_boundaries\"],\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"build_commands\": { \"type\": \"array\", \"items\": { \"type\": \"string\" } },\n        \"test_commands\":  { \"type\": \"array\", \"items\": { \"type\": \"string\" } },\n        \"entry_points\": {\n          \"type\": \"array\",\n          \"items\": {\n            \"type\": \"object\",\n            \"required\": [\"kind\", \"location\"],\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"kind\": { \"type\": \"string\", \"description\": \"http_route|cli|rpc|grpc|message_queue|cron|library_api|webhook|file_input\" },\n              \"location\": { \"type\": \"string\", \"description\": \"file:line or symbol reference\" },\n              \"auth_required\": { \"type\": \"boolean\" },\n              \"notes\": { \"type\": \"string\" }\n            }\n          }\n        },\n        \"trust_boundaries\": {\n          \"type\": \"array\",\n          \"items\": {\n            \"type\": \"object\",\n            \"required\": [\"name\", \"description\"],\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"name\": { \"type\": \"string\" },\n              \"description\": { \"type\": \"string\" },\n              \"source_zone\": { \"type\": \"string\" },\n              \"sink_zone\": { \"type\": \"string\" }\n            }\n          }\n        },\n        \"external_inputs\": {\n          \"type\": \"array\",\n          \"items\": {\n            \"type\": \"object\",\n            \"required\": [\"name\", \"kind\"],\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"name\": { \"type\": \"string\" },\n              \"kind\": { \"type\": \"string\", \"description\": \"http_param|http_header|http_body|env_var|file|cli_arg|stdin|db_field|broker_message\" },\n              \"controllable_by\": { \"type\": \"string\", \"description\": \"anonymous_user|authenticated_user|admin|internal_service\" }\n            }\n          }\n        }\n      }\n    },\n    \"initial_tasks\": {\n      \"type\": \"array\",\n      \"minItems\": 0,\n      \"description\": \"Normally 30-max_tasks tasks. In diff/PR mode this may be small or empty when the changed files expose no new attack surface.\",\n      \"items\": { \"$ref\": \"hunt_task.schema.json\" }\n    }\n  }\n}\n",
  "schemas/remediation.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"RemediationAdvice\",\n  \"description\": \"Code-grounded fix guidance for a single finding, produced by reading the actual sink + trace. Distinct from the `fix` stage, which writes a real patch — this is explanatory remediation surfaced inline in the report and viewer.\",\n  \"type\": \"object\",\n  \"required\": [\"finding_id\", \"recommendation\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"finding_id\": { \"type\": \"string\" },\n    \"root_cause\": {\n      \"type\": \"string\",\n      \"description\": \"Why the bug exists in THIS code — the specific missing guard or unsafe call, naming the real symbols.\"\n    },\n    \"recommendation\": {\n      \"type\": \"string\",\n      \"minLength\": 20,\n      \"description\": \"Concrete fix steps grounded in the actual code: name the real function/file, the framework's safe API, and the exact change to make. Not generic advice.\"\n    },\n    \"fix_sketch\": {\n      \"type\": \"object\",\n      \"description\": \"Optional minimal before/after snippet illustrating the change.\",\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"language\": { \"type\": \"string\" },\n        \"code\": { \"type\": \"string\", \"description\": \"A small illustrative snippet of the fixed code (not a full patch).\" },\n        \"note\": { \"type\": \"string\" }\n      }\n    },\n    \"references\": {\n      \"type\": \"array\",\n      \"items\": { \"type\": \"string\" },\n      \"description\": \"Authoritative links (CWE, OWASP, framework docs) relevant to this fix.\"\n    },\n    \"confidence\": { \"type\": \"number\", \"minimum\": 0, \"maximum\": 1 }\n  }\n}\n",
  "schemas/report.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"Report\",\n  \"description\": \"Final structured report — only reachable, deduped findings.\",\n  \"type\": \"object\",\n  \"required\": [\"run_id\", \"target\", \"summary\", \"findings\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"run_id\": { \"type\": \"string\" },\n    \"target\": {\n      \"type\": \"object\",\n      \"required\": [\"repo_path\"],\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"repo_path\": { \"type\": \"string\" },\n        \"commit\": { \"type\": \"string\" }\n      }\n    },\n    \"delta\": {\n      \"type\": \"object\",\n      \"description\": \"Present only when a --baseline was applied. Counts NEW vs known findings and lists those FIXED since the baseline.\",\n      \"required\": [\"baseline_total\", \"new_count\", \"still_present_count\", \"fixed_count\"],\n      \"additionalProperties\": true,\n      \"properties\": {\n        \"baseline_total\": { \"type\": \"integer\", \"minimum\": 0 },\n        \"new_count\": { \"type\": \"integer\", \"minimum\": 0 },\n        \"still_present_count\": { \"type\": \"integer\", \"minimum\": 0 },\n        \"fixed_count\": { \"type\": \"integer\", \"minimum\": 0 }\n      }\n    },\n    \"summary\": {\n      \"type\": \"object\",\n      \"required\": [\"total\", \"by_severity\"],\n      \"additionalProperties\": false,\n      \"properties\": {\n        \"total\": { \"type\": \"integer\", \"minimum\": 0 },\n        \"by_severity\": {\n          \"type\": \"object\",\n          \"additionalProperties\": false,\n          \"properties\": {\n            \"critical\":      { \"type\": \"integer\", \"minimum\": 0 },\n            \"high\":          { \"type\": \"integer\", \"minimum\": 0 },\n            \"medium\":        { \"type\": \"integer\", \"minimum\": 0 },\n            \"low\":           { \"type\": \"integer\", \"minimum\": 0 },\n            \"informational\": { \"type\": \"integer\", \"minimum\": 0 }\n          }\n        }\n      }\n    },\n    \"findings\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\"finding_id\", \"title\", \"severity\", \"vuln_class\", \"file\", \"line_start\", \"line_end\", \"description\", \"evidence\", \"trace\", \"recommendation\"],\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"finding_id\": { \"type\": \"string\" },\n          \"title\": { \"type\": \"string\", \"minLength\": 5 },\n          \"severity\": { \"type\": \"string\", \"enum\": [\"critical\", \"high\", \"medium\", \"low\", \"informational\"] },\n          \"vuln_class\": { \"type\": \"string\" },\n          \"cwe\": { \"type\": \"string\", \"pattern\": \"^CWE-[0-9]+$\" },\n          \"file\": { \"type\": \"string\" },\n          \"line_start\": { \"type\": \"integer\", \"minimum\": 1 },\n          \"line_end\": { \"type\": \"integer\", \"minimum\": 1 },\n          \"description\": { \"type\": \"string\", \"minLength\": 30 },\n          \"evidence\": { \"type\": \"string\" },\n          \"trace\": {\n            \"type\": \"object\",\n            \"required\": [\"entry_points\", \"call_chain\"],\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"entry_points\": {\n                \"type\": \"array\",\n                \"items\": {\n                  \"type\": \"object\",\n                  \"required\": [\"kind\", \"location\"],\n                  \"additionalProperties\": false,\n                  \"properties\": {\n                    \"kind\": { \"type\": \"string\" },\n                    \"location\": { \"type\": \"string\" },\n                    \"controllable_by\": { \"type\": \"string\" }\n                  }\n                }\n              },\n              \"call_chain\": {\n                \"type\": \"array\",\n                \"items\": {\n                  \"type\": \"object\",\n                  \"required\": [\"file\", \"function\", \"line\"],\n                  \"additionalProperties\": false,\n                  \"properties\": {\n                    \"file\": { \"type\": \"string\" },\n                    \"function\": { \"type\": \"string\" },\n                    \"line\": { \"type\": \"integer\", \"minimum\": 1 }\n                  }\n                }\n              }\n            }\n          },\n          \"poc\": {\n            \"type\": \"object\",\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"language\": { \"type\": \"string\" },\n              \"code\": { \"type\": \"string\" },\n              \"succeeded\": { \"type\": \"boolean\" }\n            }\n          },\n          \"variants\": {\n            \"type\": \"array\",\n            \"items\": { \"type\": \"string\" },\n            \"description\": \"Other finding_ids in the same dedupe group.\"\n          },\n          \"recommendation\": { \"type\": \"string\", \"minLength\": 20 }\n        }\n      }\n    }\n  }\n}\n",
  "schemas/trace.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"Trace\",\n  \"description\": \"Reachability verdict for a single canonical finding.\",\n  \"type\": \"object\",\n  \"required\": [\"finding_id\", \"reachable\", \"confidence\", \"rationale\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"finding_id\": { \"type\": \"string\" },\n    \"reachable\": {\n      \"type\": \"boolean\",\n      \"description\": \"True iff attacker-controlled input from an external entry point can reach the sink.\"\n    },\n    \"entry_points\": {\n      \"type\": \"array\",\n      \"description\": \"External entry points from which the sink is reachable. Empty if reachable=false.\",\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\"kind\", \"location\"],\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"kind\": { \"type\": \"string\", \"description\": \"http_route|cli|rpc|message_queue|webhook|cron|library_api|file_input\" },\n          \"location\": { \"type\": \"string\", \"description\": \"file:line or symbol\" },\n          \"auth_required\": { \"type\": \"boolean\" },\n          \"controllable_by\": { \"type\": \"string\" }\n        }\n      }\n    },\n    \"call_chain\": {\n      \"type\": \"array\",\n      \"description\": \"Ordered chain from entry to sink. First element is the entry frame; last is the sink.\",\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\"file\", \"function\", \"line\"],\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"file\": { \"type\": \"string\" },\n          \"function\": { \"type\": \"string\" },\n          \"line\": { \"type\": \"integer\", \"minimum\": 1 },\n          \"note\": { \"type\": \"string\" }\n        }\n      }\n    },\n    \"external_inputs\": {\n      \"type\": \"array\",\n      \"description\": \"Specific input parameters that flow into the sink.\",\n      \"items\": { \"type\": \"string\" }\n    },\n    \"confidence\": { \"type\": \"number\", \"minimum\": 0, \"maximum\": 1 },\n    \"rationale\": { \"type\": \"string\", \"minLength\": 20 },\n    \"blockers\": {\n      \"type\": \"array\",\n      \"description\": \"Reasons the path is NOT reachable (sanitizers, auth gates, dead code, etc.).\",\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\"kind\", \"location\", \"description\"],\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"kind\": { \"type\": \"string\", \"description\": \"sanitizer|auth_check|input_validation|dead_code|feature_flag|other\" },\n          \"location\": { \"type\": \"string\" },\n          \"description\": { \"type\": \"string\" }\n        }\n      }\n    }\n  }\n}\n",
  "schemas/triage_verdict.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"TriageVerdict\",\n  \"description\": \"Triage outcome for one inbound bug-bounty / VDP submission: reproduced?, valid? (adversarial review), reachable? (gate), duplicate?, and a recommendation.\",\n  \"type\": \"object\",\n  \"required\": [\"run_id\", \"source\", \"target\", \"reproduced\", \"recommendation\", \"validity\", \"rationale\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"run_id\": { \"type\": \"string\" },\n    \"source\": { \"type\": \"string\", \"description\": \"Where the report came from (file path or label).\" },\n    \"target\": {\n      \"type\": \"object\",\n      \"required\": [\"repo_path\"],\n      \"additionalProperties\": false,\n      \"properties\": { \"repo_path\": { \"type\": \"string\" } }\n    },\n    \"reproduced\": { \"type\": \"boolean\" },\n    \"recommendation\": {\n      \"type\": \"string\",\n      \"enum\": [\"accept\", \"reject\", \"duplicate\", \"needs_info\", \"not_reproduced\"]\n    },\n    \"validity\": {\n      \"type\": \"string\",\n      \"enum\": [\"valid\", \"invalid\", \"needs_more_info\", \"not_reproduced\"],\n      \"description\": \"Verdict of the independent adversarial reviewer.\"\n    },\n    \"reachable\": {\n      \"type\": [\"boolean\", \"null\"],\n      \"description\": \"Reachability-gate result; null when not reproduced or not traced.\"\n    },\n    \"severity\": {\n      \"type\": [\"string\", \"null\"],\n      \"enum\": [\"critical\", \"high\", \"medium\", \"low\", \"informational\", null]\n    },\n    \"duplicate_of\": {\n      \"type\": [\"object\", \"null\"],\n      \"additionalProperties\": false,\n      \"required\": [\"finding_id\", \"source\", \"fingerprint\"],\n      \"properties\": {\n        \"finding_id\": { \"type\": \"string\" },\n        \"source\": { \"type\": \"string\", \"description\": \"run:<id> or baseline:<path>\" },\n        \"fingerprint\": { \"type\": \"string\" }\n      }\n    },\n    \"fingerprint\": { \"type\": [\"string\", \"null\"] },\n    \"rationale\": { \"type\": \"string\", \"minLength\": 10 },\n    \"finding\": { \"type\": [\"object\", \"null\"] },\n    \"validation\": { \"type\": [\"object\", \"null\"] },\n    \"trace\": { \"type\": [\"object\", \"null\"] }\n  }\n}\n",
  "schemas/validation.schema.json": "{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"title\": \"ValidationVerdict\",\n  \"description\": \"Adversarial review verdict for a single finding. CANNOT emit new findings.\",\n  \"type\": \"object\",\n  \"required\": [\"finding_id\", \"verdict\", \"rationale\", \"validator_confidence\"],\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"finding_id\": { \"type\": \"string\" },\n    \"verdict\": {\n      \"type\": \"string\",\n      \"enum\": [\"confirmed\", \"rejected\", \"needs_more_info\"]\n    },\n    \"rationale\": {\n      \"type\": \"string\",\n      \"minLength\": 30,\n      \"description\": \"Why the finding stands or falls. Must engage with the evidence, not restate it.\"\n    },\n    \"alternative_explanation\": {\n      \"type\": \"string\",\n      \"description\": \"If rejected: the benign explanation. If confirmed: any rival hypothesis you ruled out.\"\n    },\n    \"missing_preconditions\": {\n      \"type\": \"array\",\n      \"items\": { \"type\": \"string\" },\n      \"description\": \"Conditions that must hold for the bug to fire (sanitization upstream, config flags, etc.).\"\n    },\n    \"suggested_test\": {\n      \"type\": \"string\",\n      \"description\": \"Concrete test that would disambiguate — e.g. a curl command, a unit test stub.\"\n    },\n    \"validator_confidence\": { \"type\": \"number\", \"minimum\": 0, \"maximum\": 1 }\n  }\n}\n"
};
